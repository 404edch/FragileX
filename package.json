{
  "name": "fragilex-root",
  "version": "1.0.0",
  "description": "Script de automação para rodar frontend e backend juntos",
  "scripts": {
    "dev": "npx concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\""
  }
}
